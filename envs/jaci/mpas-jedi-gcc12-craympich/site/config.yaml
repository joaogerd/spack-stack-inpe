# JACI environment site config include
#
# Purpose
# -------
# Store configuration specific to the concrete JACI environment instance.
#
# Context
# -------
# The validated environment spack.yaml includes this directory with:
#
#   include:
#   - site
#   - common
#
# Validation status
# -----------------
# This file is reconstructed from the JACI configuration audit. It must still be
# compared against the original validated file from:
#
#   envs/mpas-bundle-jaci-gnu12/site/config.yaml
#
# Open items
# ----------
# 1. Confirm whether the install_tree root should remain user-specific.
# 2. Confirm whether these lock timeout values are still appropriate.

config:
  build_jobs: 8
  db_lock_timeout: 60
  package_lock_timeout: 30
  install_tree:
    root: /p/projetos/monan_das/joao.gerd/env/spack-stack/install-release-2.1-gcc12
